spring:
  cloud:
    config:
      uri: http://192.168.198.138:8888
      name: service-sso, zipkin-client, database-users, mybatis, feign-client, admin-client, eureka-client
      label: master
      profile: dev